[tool.pytest.ini_options]
pythonpath = ["plugins/dev-loop"]
testpaths = ["tests"]

[tool.ruff]
target-version = "py310"

[tool.ruff.lint]
# E402(測試檔的區段式延後 import)、E702(測試 fixture 的緊湊分號風格)、
# E741(legs/units 短迴圈變數 l)為既有刻意風格,不視為問題。
ignore = ["E402", "E702", "E741"]
